Replace `logo-placeholder.svg` with the institute logo (e.g. `vnrvjiet.png`), then point
`institute.logo` in `_config.yml` at the new file. Square, at least 104x104 px, works best.

Instructor photos go here too — set `instructors[].photo` in `_data/course.yml` to
`/assets/images/your-photo.jpg`.
